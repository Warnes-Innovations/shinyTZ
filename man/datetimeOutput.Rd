% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datetime-outputs.R, R/datetimeOutput.R
\name{datetimeOutput}
\alias{datetimeOutput}
\title{Datetime output UI element}
\usage{
datetimeOutput(
  outputId,
  container = if (inline) shiny::span else shiny::div,
  placeholder = "Loading...",
  tz_display = TRUE,
  inline = FALSE
)

datetimeOutput(
  outputId,
  container = if (inline) shiny::span else shiny::div,
  placeholder = "Loading...",
  tz_display = TRUE,
  inline = FALSE
)
}
\arguments{
\item{outputId}{Output variable name}

\item{container}{A function to generate an HTML element to contain the text}

\item{placeholder}{Placeholder text shown before reactive value available}

\item{tz_display}{Show timezone abbreviation (default: TRUE)}

\item{inline}{Use an inline (\code{span()}) or block container (\code{div()})}
}
\value{
Shiny UI output element

Shiny UI output element
}
\description{
Creates a UI element for displaying timezone-aware datetime values.
Use with \code{\link{renderDatetime}} in server code.

Creates a UI element for displaying timezone-aware datetime values.
Use with \code{\link{renderDatetime}} in server code.
}
\examples{
\dontrun{
# UI
datetimeOutput("last_update")
datetimeOutput("inline_time", inline = TRUE)

# Server
output$last_update <- renderDatetime({
  Sys.time()
})
}
\dontrun{
# UI
datetimeOutput("last_update")
datetimeOutput("inline_time", inline = TRUE)

# Server
output$last_update <- renderDatetime({
  Sys.time()
})
}
}
