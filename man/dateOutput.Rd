% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dateOutput.R, R/datetime-outputs.R
\name{dateOutput}
\alias{dateOutput}
\title{Date output UI element}
\usage{
dateOutput(
  outputId,
  container = if (inline) shiny::span else shiny::div,
  placeholder = "Loading...",
  inline = FALSE
)

dateOutput(
  outputId,
  container = if (inline) shiny::span else shiny::div,
  placeholder = "Loading...",
  inline = FALSE
)
}
\arguments{
\item{outputId}{Output variable name}

\item{container}{A function to generate an HTML element to contain the text}

\item{placeholder}{Placeholder text}

\item{inline}{Use an inline (\code{span()}) or block container (\code{div()})}
}
\value{
Shiny UI output element

Shiny UI output element
}
\description{
Creates a UI element for displaying timezone-aware date values (date-only).
Use with \code{\link{renderDate}} in server code.

Creates a UI element for displaying timezone-aware date values (date-only).
Use with \code{\link{renderDate}} in server code.
}
\examples{
\dontrun{
dateOutput("processing_date")
dateOutput("inline_date", inline = TRUE)
}
\dontrun{
dateOutput("processing_date")
dateOutput("inline_date", inline = TRUE)
}
}
