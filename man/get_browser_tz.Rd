% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timezone-utils.R
\name{get_browser_tz}
\alias{get_browser_tz}
\title{Get browser timezone from session}
\usage{
get_browser_tz(
  session = shiny::getDefaultReactiveDomain(),
  fallback = Sys.timezone()
)
}
\arguments{
\item{session}{Shiny session object (default: current reactive domain)}

\item{fallback}{Fallback timezone if detection unavailable (default: Sys.timezone())}
}
\value{
Character string with IANA timezone name (e.g., "America/New_York")
}
\description{
Retrieves the timezone detected from the user's browser via JavaScript.
Falls back to server timezone if JavaScript unavailable or invalid.
}
\examples{
\dontrun{
server <- function(input, output, session) {
  user_tz <- reactive({
    get_browser_tz(session)
  })
  
  output$debug_info <- renderText({
    paste("Your timezone:", user_tz())
  })
}
}
}
